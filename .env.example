# Copy to .env and fill in values
ANTHROPIC_API_KEY=
# Optional: default when the UI does not send model — sonnet | haiku | full Claude model id
# ANTHROPIC_MODEL=sonnet
# Optional: ISO date (YYYY-MM-DD) in the assistant system prompt (defaults to server date)
# MPR_ASSISTANT_TODAY=
